package main
import(
	"fmt"
	)

type term struct{
	coef int
	pow int
}

func integrate(poly []*term, x0, x int) float{
	for i, j := range poly{
		
	}
}

func picar(x int)int{
	u0 := 0
	poly := make([]*term, 0)
	poly = append(poly, term{1, 2})
}
	
func main(){

}